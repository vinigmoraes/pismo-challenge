ktor {
    deployment {
        host = "127.0.0.1"
        port = 8080
    }
    application {
        modules = [br.com.pismochallenge.application.PismoChallengeApplicationKt.main]
    }
}
database {
    url: "jdbc:postgresql://localhost:5434/pismo-challenge"
    username: "postgres"
    password: "root"
}
